<footer class="bg-dark text-white mt-4">
  <div class="container-fluid py-3">
    <div class="row">
      <div class="col-md-6 small text-secondary">
        <% if GitInfo.instance.available? %>
          <span title="Message <%= GitInfo.instance.message %>">Commit # <%= GitInfo.instance.commit_id %>
            (in <%= GitInfo.instance.branch %>)</span>
          <span title="<%= GitInfo.instance.author[1] rescue '' %>"> by <%= GitInfo.instance.author[0].humanize rescue '' %></span>
        <% end %>
      </div>
      <div class="col-md-3"></div>
      <div class="col-md-3 text-right small align-self-end">©<%=Time.now.strftime("%Y") %>, McKinsey & Company</div>
    </div>
  </div>
</footer>