<h3>Accounts</h3>

<div class="row">

  <%= render(partial: 'accounts/new_account_card') if current_user.leading_accounts.exists? %>
  <% @accounts.each do |account| %>
    <%= render(partial: 'accounts/card', locals: {account: account}) %>
  <% end %>
</div>